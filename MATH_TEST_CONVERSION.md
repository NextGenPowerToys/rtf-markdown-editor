# Math Conversion Test

## Block Math Test
This should be converted to math block:

$$\frac{a}{b}$$

And another one:

$$x^2 + y^2 = z^2$$

## Inline Math Test
This is inline math: $E = mc^2$ in the middle of text.

And here: $\sqrt{16} = 4$ with more text after.

## Mixed Content
Hebrew text with math: תנועה מתומדת עם $v = \frac{d}{t}$ בתוך הטקסט.

Another block:
$$\text{ממוצע} = \frac{\sum_{i=1}^{n} x_i}{n}$$

This is the formula with inline: $n$ is count and $x_i$ are values.

## Complex Example
$$\int_{0}^{\infty} e^{-x^2} dx = \frac{\sqrt{\pi}}{2}$$

And inline: The constant is $\pi \approx 3.14159$.

## Edge Cases
Single dollar: This $shouldn't match a single $
Double at end: Formula $$test$$
